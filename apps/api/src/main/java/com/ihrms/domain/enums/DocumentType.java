package com.ihrms.domain.enums;

/** Uploaded document kinds (§4, extensible). PG enum type {@code "DocumentType"}. */
public enum DocumentType {
  EXPERIENCE_LETTER,
  PAN,
  AADHAAR,
  BGV_DOCUMENT,
  OTHER
}
