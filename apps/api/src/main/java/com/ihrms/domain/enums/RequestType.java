package com.ihrms.domain.enums;

/** Document-request kinds an employee asks the Accountant for (§8d). PG enum type {@code "RequestType"}. */
public enum RequestType {
  PAYSLIP,
  SALARY_CERTIFICATE,
  FORM16,
  TAX_DOCUMENT,
  OTHER
}
