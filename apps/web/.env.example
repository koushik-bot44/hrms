# Base URL of the CDPP API (no trailing slash). The web app reads this at build
# and runtime to reach the API (e.g. GET /health for the status indicator).
# Local dev: the API runs on :3000, the web app on :3001.
NEXT_PUBLIC_API_URL=http://localhost:3000
